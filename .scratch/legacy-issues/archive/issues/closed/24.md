Style makeover

Currently the buttons for adjusting role type counts are rendered like:

Count: - 
$COUNT
+


I'd prefer if it were something like

± $Count

And move the caption "count" to a more descriptive tooltip.

This will likely require testing via playwright mcp and looking at the DOM and taking and examining screenshots to get right.

STATUS: DONE


Next:

The glow on the buttons is weird and they're the same color as the background. Can't we make it more aesthetic?
Doesn't DaisyUI support themes?
